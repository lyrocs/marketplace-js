<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  @if(env('ANALYTICS_WEBSITE_ID'))
  <script defer src="{{ env('ANALYTICS_BASE_URL') }}" data-website-id="{{ env('ANALYTICS_WEBSITE_ID') }}"></script>
  @end
  
  @vite(['inertia/app/app.ts', `inertia/pages/${page.component}.vue`])
  @inertiaHead()
</head>

<body>
  @inertia()
</body>

</html>
